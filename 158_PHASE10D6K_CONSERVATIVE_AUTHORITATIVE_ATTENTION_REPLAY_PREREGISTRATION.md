# Phase 10D.6K — Conservative Authoritative Attention Replay

**Status:** PREREGISTERED / NO 10D.6K OUTCOMES SAMPLED  
**Date:** 2026-09-11

## Question

On the frozen 10D.6H cardinal-free, support-bound Relation-Mapping realizations, can authoritative grounding/epistemic bands be reconnected to the already-validated `Anchored + Magnitude-Free + Pareto` decision stack without reintroducing LLM cardinal scores?

## Frozen semantic input

Use the exact 24 10D.6H realizations (A/D/X/N4, six each), artifact SHA256 `86e1dc644aa0b7a8e6f559b2fd674357268ba12ecd82b586ed626b655965e29c`. No new acquisition, Sensor, Auditor, Locate, or Relation-Mapping call is allowed. Relation topology, target identity, support IDs, jurisdiction anchors, reasons, Kernel and Locate are frozen.

## Directness projection

For each case, deduplicate targeted effects by `(operation, target, sorted support_unit_ids)`. Run the frozen 10D.6J directness classifier five times per case on these unique signatures. Use the modal fit; ties resolve conservatively in this order: `CONTRADICTS_OPERATION > INSUFFICIENT > PARTIAL > DIRECT`. The classifier remains a research instrument, not a truth oracle.

Frozen provenance role by acquired source: A=`PRIMARY_SOURCE`, D=`SECONDARY_REPORT`, X=`PRIMARY_SOURCE`, N4=`PRIMARY_SOURCE`.

## Authority arms

All arms use the 10D.6E C1 Kernel importance policy and the exact production strategy `pareto-multidelta-magnitude-free-anchored-open-new`. Ordinal bands are encoded as 0/1 only for compatibility with the existing Magnitude-Free implementation; they are not cardinal estimates.

- `K0_ALL_SUPPORT_SUFFICIENT`: every deterministic-legal support-bound effect receives epistemic SUFFICIENT. Sensitivity upper bound only.
- `K1_SINGLE_SOURCE_WEAK`: targeted effects are WEAK on these single-source articles; legal OPEN_NEW is SUFFICIENT when support-bound. This reproduces the conservative 10D.6E direction on the new topology.
- `K2_CANONICAL_AUTHORITY`: targeted `DIRECT + PRIMARY_SOURCE` => SUFFICIENT; targeted `DIRECT + SECONDARY_REPORT` => WEAK; `PARTIAL` => WEAK; `INSUFFICIENT` and `CONTRADICTS_OPERATION` => reject. OPEN_NEW remains admitted/rejected by Anchored admission and is SUFFICIENT only for a support-bound PRIMARY_SOURCE branch, otherwise WEAK.

## Measurement

For every frozen sample and arm, persist pre-grounding relation families, rejected effects/reasons, authority trace, Pareto decision-causal core, and final article-level disposition after Anchored -> Magnitude-Free -> Pareto -> article Attention join. Compare paired dispositions across arms and contextualize against historical 10D.4 frequencies without treating those frequencies as Gold.

A promotable direction must not create new ENGAGE inflation relative to K1, must preserve no-critical-false-DROP behavior on the four sentinels, and must recover stronger treatment of direct N4 technical evidence without upgrading A/D scope extrapolations. This is a shadow replay only; production defaults remain unchanged and Phase 9A remains paused.
