# Phase 10D.6G — Native-Semantics Support-Binding Parity Shadow

**Status:** PREREGISTERED / NO 10D.6G OUTCOMES SAMPLED
**Date:** 2026-09-11

## Question

Can explicit relation-level provenance (`support_unit_ids`, `jurisdiction_anchor_ids`) be added to the historical Phase-10 native Relation-Mapping contract without materially rewriting the relation topology itself?

10D.6F proved that support binding is mechanically feasible, but it simultaneously removed the compatibility cardinal fields and strengthened scope/direct-support instructions. A/D collapsed to empty and X lost historical targeted relations, so that result is causally confounded.

## Frozen intervention

Reuse the exact frozen A/D/X/N4 audited worlds, Kernel fixtures, and modal Locate from Phase 10D.4. Use the historical `NATIVE_IMPACT_SYSTEM` semantics as the base. Add only provenance obligations:

- every emitted effect cites exact `support_unit_ids` from the frozen canonical world;
- OPEN_NEW cites at least one supplied `jurisdiction_anchor_id`;
- compatibility `change_magnitude`, `epistemic_strength`, and `target_importance` remain in the output schema for this parity shadow but are diagnostic only;
- no Attention policy or authority policy is evaluated in this gate.

Unknown support/target/anchor identifiers fail closed at effect level. Duplicate relation identities are recorded raw and also deterministically normalized; no LLM winner selection is allowed.

## Historical descriptive reference

Before sampling, the Phase 10D.4 pooled t1+t2 relation families with sample-level occurrence >= 0.50 are frozen as descriptive parity sentinels, not semantic Gold:

- A: `CHALLENGE(B1)`, `REINFORCE(M1)`, `OPEN_NEW`, `CHALLENGE(Q1)`.
- D: `CHALLENGE(B2)`, `REINFORCE(B1)`, `REINFORCE(Q1)`, `REINFORCE(BT1)`, `OPEN_NEW`, `REINFORCE(Q2)`, `REINFORCE(M1)`.
- X: `OPEN_NEW`, `REINFORCE(M1)`, `CHALLENGE(BT1)`, `REINFORCE(B1)`, `REINFORCE(Q1)`.
- N4: `OPEN_NEW`, `REINFORCE(B1)`, `REINFORCE(BT1)`, `REINFORCE(M1)`, `REINFORCE(Q1)`, `CHALLENGE(BT1)`, `CHALLENGE(B1)`.

These sentinels test contract parity only. Historically frequent relations may still be semantically weak and must not become correctness Gold merely because they are frequent.

## Sampling and outputs

Run 6 fresh Relation-Mapping realizations for each of A/D/X/N4, interleaved only by fixed case order: 24 calls total, temperature 0.1, thinking disabled, no outcome-dependent expansion.

Persist raw effects, valid effects, deterministic normalized effects, relation topology, support signatures, jurisdiction anchors, validation failures, duplicate counts, and model metadata.

Primary descriptive checks:

1. structured success 24/24;
2. effect-level identifier validity;
3. recovery of historical sentinel relation families;
4. support-binding stability for recovered targeted relations;
5. duplicate-relation burden before deterministic normalization.

No production promotion follows directly from this gate. If support binding coexists with native topology, the next problem is canonical grounding/evidence-class authority. If topology still collapses, schema/provenance obligations themselves are decision-bearing and require further decomposition.

Production defaults remain unchanged. Phase 9A remains paused.
