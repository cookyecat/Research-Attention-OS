# Phase 10D.3 — Real-Web Static Cognitive Map Batch-3 Preregistration

Status: PREREGISTERED / SELECTED BY ACQUISITION-ONLY PREFLIGHT

## Frozen selection

Batch-3 selection came only from `phase10d3-acquisition-preflight-v0.1`. Within each stratum, the first two manifest-ordered pages passing URLConnector acquisition were selected before any Sensor/Auditor/Locate/Relation/Attention call.

Selected cases: `N3`, `N4`, `B3`, `B4`, `F3`, `F4`.

Preflight artifact SHA256: `5d707a6bafd3558e9d2ba86232e9e468485776a43a4cccbd430e4b867df81ebf`.

## Measurement contract

Each selected page must match its preflight `content_hash` and `content_chars` before model calls. A mismatch is retained as a source-snapshot failure rather than silently accepting a changed page.

For each passing source: one fresh Sensor v0.2.6 + Auditor v0.1.1 perception pass, then freeze the exact admitted semantic world; Locate x3 and freeze modal target-set realization; Relation Mapping initial N=12 under the same Anchored + Magnitude-Free + Pareto research stack.
If the preregistered Wilson precision gate fails, expand that source only to N=24. No result-dependent source replacement is allowed.

Static map outputs remain:

- empirical topology relation probability `P(r in T)`;
- empirical load-bearing probability `P(r in B_pi)` using Decision-Causal Core necessary-or-sufficient support;
- Attention distribution `P(A)`;
- topology/load-bearing/Attention entropy and concentration.

OPEN_NEW identity uses only explicit references to frozen admitted unit ids; otherwise the branch remains `UNRESOLVED`.

Technical Sensor/schema failure is retained without retry inside this batch. Website acquisition failure is outside RAOS cognitive-model evaluation.

No theory, threshold, prompt, calibration, decision policy, or production default may be tuned from Batch-3 outcomes. Production remains `one-delta-v1`.
