# Phase 10D.6K — Evaluator-Capacity Bracketing Amendment

**Status:** PREREGISTERED / NO NEW 10D.6K OUTCOME YET
**Date:** 2026-09-12

## Why amend the original gate

10D.6L closed the business-logic audit and established that evaluator behavior, task information, and decision policy must be separated from the stable RAOS architecture contract. The original 10D.6K preregistration used the 10D.6J weak directness classifier as the sole canonical Grounding instrument and production's global-Locate OPEN_NEW approximation. Those assumptions are now superseded for research replay.

Frozen 10D.6H semantic input, C1 Kernel importance, cardinal-free effect existence, Magnitude-Free calibration, Pareto selection, article-level Attention join, and production defaults remain unchanged.

## Authority arms

- `K0_ALL_SUPPORT_SUFFICIENT`: original sensitivity upper bound; keep every deterministic-legal support-bound effect and mark epistemic SUFFICIENT.
- `K1_SINGLE_SOURCE_WEAK`: original conservative baseline; targeted effects WEAK, supported OPEN_NEW as originally specified.
- `K2_WEAK_EVALUATOR_AUTHORITY`: DeepSeek-Flash robustness lower bound.
- `K3_STRONG_EVALUATOR_AUTHORITY`: frozen strong-model architecture-capability upper bound.
## Evaluator responsibilities

For targeted `REINFORCE/CHALLENGE`, Grounding evaluates only relation-support fit: `DIRECT / PARTIAL / INSUFFICIENT / CONTRADICTS_OPERATION`.

For `OPEN_NEW`, Anchored evaluates only jurisdiction fit using the full semantics of the exact proposed `jurisdiction_anchor_ids`, not opaque IDs/codes and not the global-Locate approximation. This is a logical authority separation; it does not require two physical model calls in production.

Weak-arm modal decisions use three independent DeepSeek-Flash draws per case for targeted Grounding and three independent draws per case for OPEN_NEW jurisdiction. Ties resolve conservatively.

Strong-arm decisions are frozen before weak outcomes using one case-level policy derived from the already-frozen 10D.6L strong evidence/Kernel review, not tuned per stochastic realization:
- A, D: targeted relations `INSUFFICIENT`; OPEN_NEW `INSUFFICIENT_JURISDICTION`.
- X: `REINFORCE(M1)=DIRECT`; other targeted relations `INSUFFICIENT`; OPEN_NEW `VALID_JURISDICTION` for the frozen gesture/spatial-grounding branches.
- N4: `REINFORCE(B1)=DIRECT`, `REINFORCE(M1)=DIRECT`, `REINFORCE(BT1)=PARTIAL`, `REINFORCE(Q1)=PARTIAL`, `CHALLENGE(M1)=INSUFFICIENT`; frozen interface-abstraction OPEN_NEW branches `VALID_JURISDICTION`.

`PARTIAL` remains WEAK. `INSUFFICIENT/CONTRADICTS_OPERATION` are rejected. `DIRECT + PRIMARY_SOURCE` is SUFFICIENT; `DIRECT + SECONDARY_REPORT` remains WEAK. OPEN_NEW must pass jurisdiction and be support-bound; primary-source branches are SUFFICIENT, secondary-source branches WEAK.
## Measurement and interpretation

For all 24 frozen 10D.6H realizations, persist per-arm retained/rejected effects, Grounding/jurisdiction authority trace, Pareto decision-causal core, and final article Attention.

Primary comparisons:
- `K2_WEAK` vs `K3_STRONG`: how evaluator policy/capability propagates into Attention.
- `K1` vs `K3_STRONG`: whether authoritative Grounding improves direct N4 treatment without new A/D scope inflation.
- `K0` remains sensitivity context only.

No architecture change is inferred from weak-only disagreement. A strong-vs-weak Attention divergence is an evaluator/execution-policy sensitivity result; a shared incorrect result or deterministic invariant violation is the signal for architecture review.

Promotion is not automatic from this replay. A promising strong direction must introduce no new ENGAGE inflation on A/D, no critical false DROP on direct N4 evidence, and preserve causal provenance through the repaired 10D.6L path. Production defaults remain unchanged; Phase 9A remains paused until this replay closes.
